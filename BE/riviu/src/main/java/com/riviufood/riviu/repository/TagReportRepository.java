package com.riviufood.riviu.repository;

import com.riviufood.riviu.model.TagReport;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TagReportRepository extends JpaRepository<TagReport, Long> {
}
